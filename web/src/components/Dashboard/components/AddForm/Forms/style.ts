import { CSSProperties } from "react";

const style = {
  form: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  } as CSSProperties,
  field: {
    width: 600
  } as CSSProperties
}

export default style